# dsagent Project Instructions

This repository contains a Python-based `dsagent` starter project.

- The application runs with FastAPI.
- The project includes a Dockerfile and docker-compose configuration.
- Use `README.md` for startup commands.
